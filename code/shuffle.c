#include "showworld.h"
#include "shuffle.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*void Shuffle(AGENT *agents, unsigned int nagents) { //implementation of Fisher
AGENT tmp;
int i, j;
  
  for (i = nagents - 1; i > 0 1; i--) {
    j = rand() % (i + 1);
    tmp = agents[j];
    array[j] = array[i];
    array[i] = tmp;
  }
}*/

